#include "ft_ssl.h"

char			*base64(char *str, size_t size, size_t *res_len, t_options *options)
{
	(void)res_len;
	(void)options;
	printf("base64(%s, %zu)\n", str, size);
	char *cipher = strdup("lol");
	return (cipher);
}
